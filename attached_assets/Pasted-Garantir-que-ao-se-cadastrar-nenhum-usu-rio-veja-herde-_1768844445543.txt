Garantir que, ao se cadastrar, nenhum usuário veja, herde ou tenha acesso a qualquer dado de outro usuário, incluindo:

Favoritos
Anotações
Histórico de leitura
Configurações
Livros salvos
Qualquer conteúdo pessoal
🔒 Regras de Implementação (Obrigatórias)
Criação de Registro Limpo
Ao finalizar o cadastro, o sistema deve criar um perfil vazio no banco de dados (Supabase).
Nenhum dado pré-existente (ex: de outros usuários ou do ambiente de desenvolvimento) deve ser copiado, vinculado ou exibido.
Relacionamento Estrito por user_id
Toda tabela com dados pessoais (ex: favorites, notes, reading_history) deve ter uma coluna user_id.
Todas as consultas devem filtrar exclusivamente pelo auth.uid() do usuário logado.
Exemplo (SQL/Supabase):

sql:
SELECT * FROM favorites WHERE user_id = auth.uid();

SELECT * FROM favorites WHERE user_id = auth.uid();
Políticas de Segurança no Supabase (RLS – Row Level Security)
Ativar RLS em todas as tabelas de dados pessoais.
Criar políticas que permitam leitura, escrita e atualização SOMENTE quando user_id = auth.uid().
Negar qualquer acesso cruzado entre usuários.
Nenhum Dado Padrão ou Demo
Não carregar “exemplos”, “dados de teste” ou “modelos” automaticamente.
O app deve iniciar totalmente em branco (ex: tela de “Você ainda não tem favoritos”).
Teste de Validação (Checklist)
Criar dois usuários distintos (ex: userA@email.com e userB@email.com)
User A salva um versículo como favorito
User B não deve ver esse favorito em hipótese alguma
Ambos devem ver apenas seus próprios dados
⚠️ Atenção Crítica
Se o seu app atualmente mostra dados errados (ex: todos veem o mesmo email usuario@velorex.com ou os mesmos favoritos), isso indica que:

Não há filtro por user_id nas consultas, ou
As políticas de segurança do Supabase estão desativadas ou mal configuradas.
Isso é um risco grave de privacidade e deve ser corrigido antes de qualquer nova funcionalidade.

📌 Próximos Passos Recomendados
Audite seu banco de dados (Supabase):
Verifique se todas as tabelas com dados pessoais têm user_id.
Confirme se RLS está ativada e com políticas corretas.
Revise o código frontend/backend:
Nenhuma requisição deve omitir o filtro por usuário autenticado.
Faça o teste dos dois usuários (como descrito acima) antes de liberar para produção.